import styled from 'styled-components';

import { A4_WIDTH } from '/imports/pdf/core/api/constants';

export const Page = styled.section`
  position: relative;
  width: ${A4_WIDTH}px;
`;

export default Page;
